import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.Statement;
import java.util.ArrayList;

public class orientaOBJ {

    public static void main(String[] args) {

        try {
            Class.forName("com.mysql.jdbc.Driver");
            String sql = "SELECT * FROM tb_cardapios";
            Connection con = DriverManager.getConnection(
                    "jdbc:mysql://localhost:3306/restaurante_a3", "root", "novasenhadaa3");
            Connection conn;
            Statement st = conn.createStatement();
            ResultSet rs = st.executeQuery(sql);

            ArrayList<String> arr = new ArrayList<String>();
            while (rs.next()) {
                //O add não está funcionando com um getInt.
                arr.add(rs.getInt("cardapio_id"));
                arr.add(rs.getString("prato"));
                arr.add(rs.getString("desc_ingredientes"));
                System.out.println(arr.get(arr.size() - 1));
            }

        } catch (

        Exception asd) {
            System.out.println(asd);
        }
    }
}